%h3
  Artists
-artists.each do |artist|
  .artist
    %a{href: "#{url_for "/artist/#{artist.id}"}"}
      =artist.name
%h3
  Albums
-albums.each do |album|
  .item
    %a{href: "#{url_for "/album/#{album.id}"}"}
      %img{src: "#{url_for "/images/#{album.image_path}"}"}
